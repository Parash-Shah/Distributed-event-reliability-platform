# Distributed-event-reliability-platform
Build a production-style backend system that ingests events, processes them asynchronously, detects dropped/failed events, retries failures, and exposes operational metrics.
